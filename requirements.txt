# Audiobook Creator TTS Production Dependencies
# Install with: pip install -r requirements.txt

# SYSTEM PACKAGES (not pip-installable, install separately):
# - tkinter: Required for file browser (optional)
#   macOS (Homebrew): brew install python-tk@3.11
#   Ubuntu/Debian: sudo apt-get install python3-tk
#   File browser can be skipped - other input methods available
# - ffmpeg: Required for M4B audiobook creation (optional but recommended)
#   macOS: brew install ffmpeg
#   Ubuntu: sudo apt-get install ffmpeg
# - AtomicParsley: Required for cover art embedding (optional)
#   macOS: brew install atomicparsley

# HTTP Requests
requests==2.32.5

# Browser Automation
playwright==1.51.0

# Document Processing - PDF
pypdf==5.2.0

# Document Processing - EPUB
ebooklib==0.18
beautifulsoup4==4.13.1

# Document Processing - DOCX (optional but recommended)
python-docx==1.2.0

# Document Processing - TXT encoding detection (optional but recommended)
chardet==5.2.0

# Document Processing - Markdown (optional but recommended)
mistune==3.1.1

# Dependencies (automatically installed with above packages)
certifi==2025.10.5
charset-normalizer==3.4.4
idna==3.11
urllib3==2.5.0
soupsieve==2.6
lxml==5.3.1
